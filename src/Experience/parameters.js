export const cameraParameters = {
    enablePan: false,
    enableZoom: false,
    enableRotate: false,
    enableDamping: false,
}

export const rendererParameters = {
    clearColor: '#26132f',
}

export const materialParameters = {
    color: '#ff794d',
}
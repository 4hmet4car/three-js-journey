export default {
    camera: {
        enablePan: true,
        enableZoom: true,
        enableRotate: true,
    },

    renderer: {
        clearColor: '#000000',
    },

    lightMaterial: {
        color: '#ffffff',
        ambientLightColor: '#ffffff',
        ambientLightIntensity: 0.03,
    }
}